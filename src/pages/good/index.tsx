import { Goods } from "../../components/goods";
import { Layout } from "../../components/layout";

export const GoodPage = () => {
    return (
        <Layout>
            <Goods />
        </Layout>
    );
};
